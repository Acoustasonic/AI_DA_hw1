# 围棋 AI 项目报告

## 1. 项目目标

本项目围绕 5x5 围棋环境，实现了三个层次的智能体与一个可演示的人机界面：
- 必做部分一：随机合法落子的 `RandomAgent`
- 必做部分二：基于蒙特卡洛树搜索的 `MCTSAgent`
- 选做部分：基于 `Minimax + Alpha-Beta` 的 `MinimaxAgent`
- 可视化部分：基于 Tkinter 的图形界面 `gui.py`

项目目标不仅是让 AI 能合法下棋，还要在给定规则框架上体现搜索算法的基本思想，并通过实验比较不同方法的行为差异。

## 2. 系统结构

项目主要由以下模块组成：

- `dlgo/`
  - 提供围棋底层规则，包括棋盘表示、合法性判断、提子、劫争检测、终局判定和计分。
- `agents/random_agent.py`
  - 从所有合法着法中随机选点，主要用于验证规则调用链是否正确。
- `agents/mcts_agent.py`
  - 实现 MCTS 的四个核心阶段，并加入启发式 rollout、深度限制和残局 pass 策略。
- `agents/minimax_agent.py`
  - 实现 Minimax、Alpha-Beta 剪枝、简单局面评估和置换表缓存。
- `play.py`
  - 提供命令行对弈入口，用于批量实验与统计。
- `gui.py`
  - 提供图形界面，支持人机对弈、新游戏和停一手。

## 3. 算法设计

### 3.1 随机 AI

随机智能体的实现最直接：
- 使用 `game_state.legal_moves()` 枚举当前所有合法着法。
- 默认不主动选择 `resign`，避免测试过程中随机认输。
- 使用 `random.choice()` 在合法着法中均匀采样。

该智能体不具备策略性，但非常适合作为基线对手，用于验证规则框架和更复杂搜索算法的有效性。

### 3.2 MCTS AI

MCTS 智能体按标准四阶段实现：

1. `Selection`
   - 从根节点出发，使用 UCT 在已展开子节点中递归选择最优分支。
   - 节点价值定义为当前节点行动方视角下的平均胜率。

2. `Expansion`
   - 对叶节点生成可选合法子节点。
   - 每个子节点记录对应走法、访问次数、累计价值和启发式先验。

3. `Simulation`
   - 从新节点继续进行快速 rollout。
   - rollout 不采用完全随机策略，而是结合提子、气数、连络和位置局部性进行启发式走子。

4. `Backup`
   - 将 rollout 结果从叶节点向根节点反向传播。
   - 每向上一层切换一次“当前行动方”，价值按 `1 - value` 翻转。

最终落子时，优先选择访问次数最多的子节点；当温度参数不为 1 时，也支持按访问次数分布进行采样。

### 3.3 MCTS 优化

为了满足作业中“至少两种优化”的要求，并兼顾 5x5 小棋盘的运行效率，我加入了以下优化：

#### 1. 启发式 rollout

rollout 阶段不再完全随机，而是优先考虑：
- 能提子或打吃的落点
- 新落子后气数较多的点
- 与己方已有棋子相邻、利于连络的点
- 棋盘中部附近的点

这样可以显著减少纯随机模拟中的低质量落子，使搜索统计更稳定。

#### 2. 限制模拟深度

`play.py` 中默认给 MCTS 设置：
- `num_rounds = 60`
- `rollout_depth = 20`

也就是说，每次决策执行 60 轮模拟，每轮 rollout 最多向后展开 20 步。到达深度上限后，使用快速局面评估函数估计胜率，而不是一直模拟到完全终局。

#### 3. 启发式先验与残局 pass 处理

标准 UCT 中所有子节点通常被一视同仁。本实现中，为不同落子赋予了简单先验：
- 提子、连络、局部应手得到更高先验
- `pass` 的先验较低，只在残局阶段显著参与搜索

另外，为了减少 5x5 小棋盘上的“无意义填空”，我增加了残局判定逻辑：
- 当空点较少、无明显提子机会且最佳局部着法分数很低时，允许主动 `pass`
- 当局面已拖入收官尾声时，强制提高 `pass` 倾向，减少搜索在低价值填点上浪费时间

这一点对 `mcts vs mcts` 的收官尤其重要。

### 3.4 Minimax（选做）

选做部分实现了 Minimax 搜索，并在此基础上加入 Alpha-Beta 剪枝。

#### 搜索方法

- 在 `select_move()` 中对所有候选着法逐一展开。
- 对后继局面调用 `alphabeta()`。
- 最大层表示“根玩家”收益最大化，最小层表示对手收益最大化。

#### 评估函数

局面评估采用轻量启发式：
- 地盘差
- 棋子数量差
- 棋串总气数差

评估值统一从根玩家视角计算。若已到终局，则返回大正值或大负值，确保终局优先级高于普通中间局面。

#### Alpha-Beta 剪枝

在搜索过程中维护 `alpha` 与 `beta`：
- 最大层：若 `value >= beta`，则直接剪枝
- 最小层：若 `value <= alpha`，则直接剪枝

#### 置换表缓存

用 `(next_player, zobrist_hash)` 作为缓存键，保存：
- 已评估深度
- 局面值
- 标记位

这样可以减少重复局面的重复计算。

#### Minimax 的残局 pass 处理

和 MCTS 一样，Minimax 在空点较少、无提子机会且当前已领先时，也会主动考虑 `pass`，减少小盘面中的冗余收官。

## 4. 图形界面设计

项目使用 Tkinter 实现了一个简洁的人机对弈界面，主要功能如下：

- 显示 5x5 或 9x9 棋盘
- 显示黑白棋子与最后一手标记
- 鼠标点击交叉点落子
- 可选择人类执黑或执白
- 可选择对手为 `random / mcts / minimax`
- 支持“新游戏”和“停一手”
- 界面底部显示当前轮次状态与粗略计分信息

AI 落子通过后台线程完成，避免界面在思考期间卡死。

截图占位：
- 请在提交前插入 `gui.py` 运行截图
- 建议附一张开局图和一张终局图

## 5. 实验设置

实验环境如下：

- 操作系统：Windows
- Python 版本：`Python 3.11.4`
- 棋盘大小：`5x5`
- MCTS 参数：`num_rounds = 60`，`rollout_depth = 20`
- Minimax 参数：`max_depth = 3`
- 每组实验对局数：`3`

使用的典型命令如下：

```bash
python play.py --agent1 mcts --agent2 random --size 5 --games 3 --quiet
python play.py --agent1 minimax --agent2 random --size 5 --games 3 --quiet
python play.py --agent1 mcts --agent2 minimax --size 5 --games 3 --quiet
python play.py --agent1 minimax --agent2 mcts --size 5 --games 3 --quiet
python play.py --agent1 mcts --agent2 mcts --size 5 --games 3 --quiet
```

## 6. 实验结果

### 6.1 MCTS vs Random

| 对局设置 | 黑方胜 | 白方胜 | 平局 | 平均步数 | 平均用时 |
| --- | --- | --- | --- | --- | --- |
| MCTS(黑) vs Random(白) | 3 | 0 | 0 | 35.0 | 12.31s |

### 6.2 Minimax vs Random

| 对局设置 | 黑方胜 | 白方胜 | 平局 | 平均步数 | 平均用时 |
| --- | --- | --- | --- | --- | --- |
| Minimax(黑) vs Random(白) | 3 | 0 | 0 | 33.0 | 3.92s |

### 6.3 MCTS vs Minimax

| 对局设置 | 黑方胜 | 白方胜 | 平局 | 平均步数 | 平均用时 |
| --- | --- | --- | --- | --- | --- |
| MCTS(黑) vs Minimax(白) | 0 | 3 | 0 | 31.3 | 16.15s |
| Minimax(黑) vs MCTS(白) | 1 | 2 | 0 | 41.0 | 18.05s |

### 6.4 MCTS 收官调优结果

| 对局设置 | 黑方胜 | 白方胜 | 平局 | 平均步数 | 平均用时 |
| --- | --- | --- | --- | --- | --- |
| MCTS(黑) vs MCTS(白) | 2 | 1 | 0 | 41.3 | 23.72s |

与早期版本相比，`MCTS vs MCTS` 的收官阶段已经明显缩短，强制结算触发频率下降，说明残局 `pass` 策略对小棋盘环境有效。

## 7. 结果分析

### 7.1 MCTS 与随机策略的差异

`MCTS` 对 `Random` 取得 3:0，说明即使只使用轻量 rollout 和有限轮数搜索，只要能不断统计“哪些分支更容易赢”，就能明显优于纯随机走子。

其原因主要有两点：
- 搜索会在多轮模拟中逐渐集中到更有希望的分支
- 启发式 rollout 提高了模拟质量，使统计结果更稳定

### 7.2 Minimax 的表现

`Minimax` 对 `Random` 同样取得 3:0，而且平均用时更短。这是因为：
- 5x5 棋盘状态空间较小
- 搜索深度只有 3 层，Alpha-Beta 剪枝能有效减少分支
- 启发式评估函数对小盘面有较强区分能力

### 7.3 MCTS 与 Minimax 的比较

实验中 `Minimax` 在一部分对局中优于 `MCTS`。这说明在当前配置下：
- 小棋盘、浅层搜索时，Minimax 的静态评估函数很有竞争力
- MCTS 的强度仍然受限于模拟轮数和 rollout 质量

但两者本质不同：
- `Minimax` 更依赖评估函数质量，适合分支较少、可控深度的小盘面
- `MCTS` 更适合大搜索空间，未来可继续通过更强策略网络或价值网络增强

### 7.4 收官行为分析

早期版本的 MCTS 容易在 5x5 棋盘后期反复填空或进行低价值交换，导致对局拖长。调优后加入了：
- 低价值着法识别
- 空点数量阈值
- 残局主动 `pass`

这些改动使得收官效率明显提升，更符合真实围棋中“局势已定时尽快终局”的策略。

### 7.5 5x5 小棋盘的特殊性

5x5 围棋与标准 19x19 围棋差异很大：
- 局面更拥挤
- 开局很快进入接触战
- 地盘和棋子效率更容易直接影响结果
- 黑棋先手优势更明显

因此实验结果更适合作为“算法原理演示”，而不是对真实围棋强度的完整评估。

## 8. 与 AlphaGo / AlphaZero 的比较

本项目中的 MCTS 与 AlphaGo / AlphaZero 有明显差距：

### 8.1 搜索信息来源不同

- 本项目：节点扩展和 rollout 主要依赖手工启发式
- AlphaGo / AlphaZero：依赖策略网络给出先验概率，价值网络直接评估局面

### 8.2 是否使用学习

- 本项目：没有训练过程
- AlphaGo / AlphaZero：通过监督学习、自对弈强化学习不断提升策略与价值估计

### 8.3 搜索规模不同

- 本项目：单步仅几十轮模拟
- AlphaGo / AlphaZero：单步可进行大量模拟，并结合高性能硬件

### 8.4 rollout 方式不同

- 本项目：仍保留轻量 rollout
- AlphaZero：通常不依赖传统 rollout，而是用价值网络替代深度随机模拟

因此，本项目更像是“简化版 MCTS 围棋原型”，重点在于理解博弈搜索的基本机制，而不是复现工业级围棋 AI。

## 9. 总结

本项目完成了作业要求中的主要内容：

- 实现了随机围棋 AI
- 实现了带启发式优化的 MCTS AI
- 额外实现了 Minimax + Alpha-Beta + 缓存
- 实现了可运行的人机图形界面
- 通过实验比较了不同方法的行为差异

从实验结果看：
- `Random` 只能作为规则验证基线
- `Minimax` 在小棋盘浅搜索设置下表现较强
- `MCTS` 在加入合适的 rollout 与残局策略后，已经能稳定完成对局并取得较好效果

后续还可以继续改进的方向包括：
- 引入更强的局面评估函数
- 对 MCTS 加入 RAVE、PUCT 等策略
- 用神经网络替代手工启发式
- 扩展到 9x9 甚至更大棋盘

## 10. 附录

### 10.1 关键运行命令

```bash
python play.py --agent1 random --agent2 random --size 5
python play.py --agent1 mcts --agent2 random --size 5
python play.py --agent1 minimax --agent2 mcts --size 5
python gui.py
```

### 10.2 说明

- `play.py` 中加入了对超长对局的保护：当步数超过 `board_size * board_size * 2` 时，按当前盘面强制结算。
- 当前版本已经显著减少该保护的触发频率，但在更复杂参数下仍可能偶发触发。
